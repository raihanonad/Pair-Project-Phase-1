<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    <title>Home</title>
    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <!-- Optional: Add your custom styles here -->
    <style>
      body {
        background-color: #f8f9fa;
      }

      .center-content {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
      }

      img {
        max-width: 100%;
        margin-bottom: 20px;
        border: 1px solid #dee2e6; /* Add a border to the QR code image */
        border-radius: 5px; /* Optional: Add border-radius for a rounded look */
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* Optional: Add a subtle box shadow */
      }

      .btn-back {
        margin-top: 20px; /* Add some spacing above the "Back" button */
      }
    </style>
  </head>

  <body>
    <div class="container mt-3 center-content">
      <h2 class="text-center mb-4">Thank you for your purchase, Hun!</h2>
      <img src="<%= qr_code %>" alt="Purchase QR Code" />
      <a href="/products" class="btn btn-primary btn-back">Back</a>
      <!-- Add your home page content here -->
    </div>

    <!-- Bootstrap JS (optional, but required for certain features like the dropdowns) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
